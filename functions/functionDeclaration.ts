function add(a: number, b: number): number {
  return a + b;
}
console.log(add(2, 4));

// a: number, b: number → Explicitly type the parameters.
// : number → The function returns a number.

function Sub(a: number, b: number): number {
  return a - b;
}

console.log(Sub(7, 3));
